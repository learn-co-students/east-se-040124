

const Header = () => {
  return (
    <header>
      <h1>
        <span className="logo">{"//"}</span>
        Project Showcase
      </h1>
      <button>{true ? "Light Mode" : "Dark Mode"}</button>
    </header>
  );
}

export default Header;
